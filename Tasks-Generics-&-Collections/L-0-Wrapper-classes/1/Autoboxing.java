/**
 * The Solution class has 8 variables of primitive types with names that 
 * follow the template <type>Value.
 * For each of these variables, you need to create wrapper objects and 
 * assign them names like <type>ValueBox.
 * 
 * To create wrapper objects, use the Type.valueOf(primitive) method.
 *
 * Example of a wrapper object variable: Integer integerValueBox = ...;
 *
 * Requirements:
 * •	The Solution class should contain 8 variables of the primitive data types, 
 *      with names that follow the template "typeValue".
 * •	The Solution class should contain 8 objects of the primitive wrapper classes, 
 *      with names that follow the template "typeValueBox".
 * •	When creating wrapper objects, you need to use the existing variables of 
 *      primitive types and the Type.valueOf(primitive) method.
 */


/* 
Autoboxing
*/

public class Solution {
    byte byteValue;
    short shortValue;
    int intValue;
    long longValue;

    float floatValue;
    double doubleValue;

    char charValue;
    boolean booleanValue;

    //write your code here
}
