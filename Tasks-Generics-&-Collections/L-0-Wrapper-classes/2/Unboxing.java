/**
 * The Solution class should have 8 objects of the primitive wrapper types, 
 * with that follow the template <type>ValueBox.
 * For each of these wrapper objects, you need to create variables of the corresponding 
 * primitive types and assign them names that follow the template <type>Value.
 * When creating a variable, use the wrapper object's <type>ValueBox.primitiveValue() method.
 *
 * Example of a variable of a primitive type: int intValue = ...;
 * 
 * Requirements:
 * •	The Solution class should contain 8 objects of the primitive wrapper classes, with names that follow the template "typeValueBox".
 * •	The Solution class should contain 8 variables of the primitive data types, with names that follow the template "typeValue".
 * •	When creating variables of primitive types, you need to use the existing wrapper objects and the typeValueBox.primitiveValue() method.
 */

/* 
Unboxing
*/

public class Solution {
    Byte byteValueBox;
    Short shortValueBox;
    Integer integerValueBox;
    Long longValueBox;

    Float floatValueBox;
    Double doubleValueBox;

    Character characterValueBox;
    Boolean booleanValueBox;

    //write your code here
}
